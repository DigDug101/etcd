description "daemon to report back hot memcached keys in a graphite-friendly format"

start on networking
stop on runlevel [!2345]

respawn
respawn limit 10 5

setuid Debian-etcd

exec /usr/bin/etcd -c /etc/etcd/etcd.conf
